<!--<table datatable class="table table-striped">
    <thead>
        <tr>
            <th *ngFor="let item of _header">{{item}}</th>
        </tr>
    </thead>
    <tbody>
      <tr *ngFor="let item of _data">
        <td *ngFor="let subitem of item.createDataRow(_header)">{{subitem}}</td>
      </tr>
    </tbody>
</table>
-->

<table datatable>
  <thead>
    <tr>
      <th *ngFor="let item of _header">{{item}}</th>
      <th>1</th>
      <th>1</th>
      <th>1</th>
    </tr>
  </thead>
  <tbody>
    <tr>
      <td>1</td>
      <td>1</td>
      <td>1</td>
    </tr>
  </tbody>
</table>
