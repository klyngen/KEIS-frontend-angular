import { EndTimeDirective } from './end-time.directive';

describe('EndTimeDirective', () => {
  it('should create an instance', () => {
    const directive = new EndTimeDirective();
    expect(directive).toBeTruthy();
  });
});
